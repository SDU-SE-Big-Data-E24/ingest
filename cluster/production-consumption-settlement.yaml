---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: production-consumption-settlement-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-consumption-settlement-data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: production-consumption-settlement-data
  template:
    metadata:
      labels:
        app: production-consumption-settlement-data
    spec:
      containers:
        - name: production-consumption-settlement-data
          image: ghcr.io/sdu-se-big-data-e24/ingest-productionconsumptionsettlement:latest
          imagePullPolicy: Always
          command: ["sleep", "infinity"]
          volumeMounts:
            - name: production-consumption-settlement-data-storage
              mountPath: /root/code
      volumes:
        - name: production-consumption-settlement-data-storage
          persistentVolumeClaim:
            claimName: production-consumption-settlement-data-pvc