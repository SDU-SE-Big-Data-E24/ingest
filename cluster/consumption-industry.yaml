---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: consumption-industry-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumption-industry-data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumption-industry-data
  template:
    metadata:
      labels:
        app: consumption-industry-data
    spec:
      containers:
        - name: consumption-industry-data
          image: ghcr.io/sdu-se-big-data-e24/ingest-consumptionindustry:latest
          imagePullPolicy: Always
          command: ["sleep", "infinity"]
          volumeMounts:
            - name: consumption-industry-data-storage
              mountPath: /root/code
      volumes:
        - name: consumption-industry-data-storage
          persistentVolumeClaim:
            claimName: consumption-industry-data-pvc